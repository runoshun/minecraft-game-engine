# Historical Presentation API Notes

> **Superseded:** this document describes the retired Java/Fabric host-runtime presentation API. The current repository contains only the Node portable compiler and vanilla datapack backend. Current game-facing presentation semantics are defined in `docs/architecture.md` and the portable ADRs. This file is retained only as design history for why the bounded portable presentation primitives look the way they do.
