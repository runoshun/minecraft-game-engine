name: build

on:
  pull_request:
  push:
    branches: ['**']
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v6

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'microsoft'

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew build

      - name: Upload build artifact
        uses: actions/upload-artifact@v7
        with:
          name: mc-game-runtime
          path: build/libs/mc-game-runtime-*.jar

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          RUNTIME_JAR=$(find build/libs -maxdepth 1 -name 'mc-game-runtime-*.jar' ! -name '*-sources.jar' -print -quit)
          test -n "$RUNTIME_JAR"
          sha256sum "$RUNTIME_JAR" > "$RUNTIME_JAR.sha256"
          gh release create "${GITHUB_REF_NAME}" \
            "$RUNTIME_JAR" \
            "$RUNTIME_JAR.sha256" \
            --title "MC Game Runtime ${GITHUB_REF_NAME}" \
            --generate-notes
